spring:
  data:
    mongodb:
      database: InzApp
      host: localhost
      port: 27017

server:
  port: 8080
#  ssl:
#    key-store: classpath:balans.p12
#    key-store-password: balans
#    keyStoreType: PKCS12
#    keyAlias: balans
